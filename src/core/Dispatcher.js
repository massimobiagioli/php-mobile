export default function(clazz, event, id) {
  console.log(clazz);
  console.log(event);
  console.log(id);
  /*const pilot = new Pilot();
  const request = {
    'event': event,
    'id': id
  };
  pilot.parseEvent(request);*/
}
